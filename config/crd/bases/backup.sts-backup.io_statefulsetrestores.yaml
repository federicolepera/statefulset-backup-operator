---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: statefulsetrestores.backup.sts-backup.io
spec:
  group: backup.sts-backup.io
  names:
    kind: StatefulSetRestore
    listKind: StatefulSetRestoreList
    plural: statefulsetrestores
    singular: statefulsetrestore
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StatefulSetRestore is the Schema for the statefulsetrestores
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StatefulSetRestore
            properties:
              backupName:
                description: |-
                  BackupName specifies the name of the backup to restore from
                  If not specified, use UseLatestBackup instead
                type: string
              pvcDeletionTimeoutSeconds:
                description: |-
                  PvcDeletionTimeoutSeconds specifies the timeout for PVC deletion during restore
                  Default: 60 seconds
                format: int64
                type: integer
              scaleDown:
                description: |-
                  ScaleDown determines whether to scale the StatefulSet to 0 before restore
                  Default: true (recommended for consistency)
                type: boolean
              snapshotNames:
                description: |-
                  SnapshotNames specifies a list of specific snapshots to restore (one per PVC)
                  Example: ["nginx-backup-1234567890-data-0", "nginx-backup-1234567890-data-1"]
                  Useful for selective restore (e.g., restore only pod-0's PVC)
                  If not specified, restores all PVCs of the StatefulSet
                items:
                  type: string
                type: array
              statefulSetRef:
                description: StatefulSetRef references the StatefulSet to restore
                properties:
                  name:
                    description: Name of the StatefulSet
                    type: string
                  namespace:
                    description: Namespace where the StatefulSet is located
                    type: string
                type: object
              useLatestBackup:
                description: |-
                  UseLatestBackup restores from the most recent backup
                  If true, ignores BackupName and uses the latest available backup
                type: boolean
            required:
            - statefulSetRef
            type: object
          status:
            description: status defines the observed state of StatefulSetRestore
            properties:
              completionTime:
                description: CompletionTime is when the restore operation completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the current state of the restore
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              originalReplicas:
                description: OriginalReplicas stores the original replica count (used
                  for scaling up after restore)
                format: int32
                type: integer
              phase:
                description: Phase represents the current phase of the restore operation
                type: string
              restoredSnapshots:
                description: RestoredSnapshots contains information about restored
                  snapshots
                items:
                  description: RestoredSnapshotInfo contains information about a restored
                    snapshot.
                  properties:
                    pvcName:
                      description: PVCName is the name of the PVC that was restored
                      type: string
                    restoreTime:
                      description: RestoreTime is when the snapshot was restored
                      format: date-time
                      type: string
                    snapshotName:
                      description: SnapshotName is the name of the snapshot that was
                        restored
                      type: string
                    success:
                      description: Success indicates whether the restore was successful
                      type: boolean
                  required:
                  - pvcName
                  - restoreTime
                  - snapshotName
                  - success
                  type: object
                type: array
              startTime:
                description: StartTime is when the restore operation started
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
