---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: statefulsetbackups.backup.sts-backup.io
spec:
  group: backup.sts-backup.io
  names:
    kind: StatefulSetBackup
    listKind: StatefulSetBackupList
    plural: statefulsetbackups
    singular: statefulsetbackup
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StatefulSetBackup is the Schema for the statefulsetbackups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of StatefulSetBackup
            properties:
              postBackupHook:
                description: |-
                  PostBackupHook defines a command to execute after taking the backup
                  Useful for cleanup or notifications
                properties:
                  command:
                    description: Command is the array of command and arguments to
                      execute
                    items:
                      type: string
                    type: array
                  containerName:
                    description: ContainerName specifies the container in which to
                      run the command (optional)
                    type: string
                  timeoutSeconds:
                    description: TimeoutSeconds specifies the timeout for the command
                      execution (optional)
                    format: int64
                    type: integer
                type: object
              preBackupHook:
                description: |-
                  PreBackupHook defines a command to execute before taking the backup
                  Useful for flushing caches, creating consistent snapshots, etc.
                properties:
                  command:
                    description: Command is the array of command and arguments to
                      execute
                    items:
                      type: string
                    type: array
                  containerName:
                    description: ContainerName specifies the container in which to
                      run the command (optional)
                    type: string
                  timeoutSeconds:
                    description: TimeoutSeconds specifies the timeout for the command
                      execution (optional)
                    format: int64
                    type: integer
                type: object
              retentionPolicy:
                description: RetentionPolicy specifies how many backups to keep
                properties:
                  keepDays:
                    description: KeepDays specifies how many days to keep backups
                      (optional, not yet implemented)
                    type: integer
                  keepLast:
                    description: KeepLast specifies how many recent backups to keep
                      per PVC
                    type: integer
                type: object
                x-kubernetes-validations:
                - message: Exactly one of keepLast or keepDays must be set
                  rule: (has(self.keepLast) && !has(self.keepDays)) || (!has(self.keepLast)
                    && has(self.keepDays))
              schedule:
                description: |-
                  Schedule is a cron expression for scheduled backups (e.g., "0 2 * * *" for daily at 2 AM)
                  If empty, the backup is created only once manually
                type: string
              statefulSetRef:
                description: StatefulSetRef references the StatefulSet to backup
                properties:
                  name:
                    description: Name of the StatefulSet
                    type: string
                  namespace:
                    description: Namespace where the StatefulSet is located
                    type: string
                type: object
              volumeSnapshotClassName:
                description: VolumeSnapshotClassName is the VolumeSnapshotClass to
                  use for creating volume snapshots
                type: string
            type: object
          status:
            description: status defines the observed state of StatefulSetBackup
            properties:
              conditions:
                description: Conditions represent the current state of the backup
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastBackupTime:
                description: LastBackupTime is the timestamp of the most recent backup
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the backup operation
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
